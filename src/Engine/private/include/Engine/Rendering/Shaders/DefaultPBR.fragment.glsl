#version 420

#include "PBRTemplate.fragment.glsl"

//uniform float roughness = 0.f;
//
//uniform float metallic = 0.f;
//
//uniform vec3 baseColor = vec3(0.f, 0.f, 0.f);

void fragmentShader(in FragmentShaderInput shaderInput, inout FragmentShaderOutput shaderOutput)
{
//	shaderOutput.baseColor	= baseColor;
//	shaderOutput.roughness	= roughness;
//	shaderOutput.metallic	= metallic;
}
